# Database Migrations

All migrations have moved to `/supabase/migrations/` — the canonical location.

Apply by pasting the relevant `.sql` file into the Supabase SQL Editor,
or run `supabase db push` from the project root once the CLI is configured.
